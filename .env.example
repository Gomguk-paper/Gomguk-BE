POSTGRES_USER=postgres
POSTGRES_PASSWORD=input_your_password
POSTGRES_DB=gomguk-db
POSTGRES_PORT=5432

# --- Backend (FastAPI) ---
# Required: backend/app/core/config.py reads these from `Gomguk-BE/.env`
ACCESS_TOKEN_EXPIRE_MINUTES=60
FRONTEND_HOST=http://localhost:8080
PROJECT_NAME=Gomguk
# If you run Postgres via `docker compose up -d db`, localhost works (port is published).
DATABASE_URL=postgresql+psycopg://postgres:input_your_password@localhost:5432/gomguk-db
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/oauth/google/callback
SECRET_KEY=changeme
ALGORITHM=HS256
BACKEND_PUBLIC_URL=http://localhost:8000

AIRFLOW_FERNET_KEY=fernetkey
AIRFLOW_UID=1001

AIRFLOW_POSTGRES_PASSWORD=input_your_password
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=input_your_password


MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
